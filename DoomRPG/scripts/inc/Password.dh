#ifndef _PASSWORD_DH_
#define _PASSWORD_DH_

#include "BitIO.h"

#define PW_VERSION              5
#define PW_SIZE                 45
#define PW_MAX_ASSEMBLIES       34
#define PW_COMPAT_EXTRAS        1
#define PW_COMPAT_DOOMRL        2

static BitIO_State State;

extern "ACS"
{
    script void SaveCharacter(bool) net;
    script void LoadCharacter() net;
    script void DumpCharacter();
};

extern
{
    function int GetBitSize(int);
    function bool CheckCompatibility(int);
};

#endif
