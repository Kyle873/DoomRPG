#ifndef _PASSWORD_DH_
#define _PASSWORD_DH_

#include "BitIO.h"
#include "Defs.dh"

static BitIO_State State;
int Checksum;

extern "ACS"
{
    script void GetPassword();
    script void PasswordSetData(int Index, int Data0, int Data1, int Data2);
    script void PasswordExecute();
    script void PasswordDump();
};

#endif
