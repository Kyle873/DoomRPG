#ifndef _NAMEGEN_DH_
#define _NAMEGEN_DH_

extern
{
    function str GenerateName(str, str);
    function str GenerateMonsterName();
    function str GenerateZombieName(bool);
    function str GenerateMarineName(bool);
    function str GeneratePowersuitName();
};

#endif
