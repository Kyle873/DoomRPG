#ifndef _SHIELD_DH_
#define _SHIELD_DH_

#include "Defs.dh"

library("Shield");

extern
{
	variable map_array str[4][MAX_PARTS][4] ShieldData;
	variable map_array int[3][MAX_PARTS][3] ShieldStats;
	variable map_array int[4] ShieldPartsMax;
};

extern "ACS"
{
	script void ToggleShield(bool);
	script void AddShield(int);
};

extern
{
	script void ShieldScript();
	script void AddShield(int);
	function void CheckShields();
	function void CheckShieldAccessory();
};

#endif
