#ifndef _TURRET_DH_
#define _TURRET_DH_

#include "Defs.dh"

extern "ACS"
{
    script void TurretMenu() enter;
    script void TurretSetOffset(int, int, int);
    script void TurretInit(int);
};

extern
{
    script void TurretLoop();
    
    function void TurretSpawn();
    function void TurretDespawn();
};

#endif
