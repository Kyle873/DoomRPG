#ifndef _TURRET_DH_
#define _TURRET_DH_

extern variable MapArray TurretUpgrade[MAX_UPGRADES] TurretUpgradeData;

extern "ACS"
{
    script void TurretMenu() enter;
    script void TurretSetOffset(int, int, int);
    script void TurretInit();
};

extern
{
    script void TurretLoop();
    
    function void TurretSpawn();
    function void TurretDespawn();
};

#endif
