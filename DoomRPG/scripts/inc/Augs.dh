#ifndef _AUGS_DH_
#define _AUGS_DH_

#include "Defs.dh"

library("Augs");

extern
{
	variable map_array str[MAX_AUGS][AUG_LEVELS + 1] AugData;
	variable map_array int[MAX_AUGS] AugLevelMax;
};

extern
{
	function void CheckAugSlots();
	function void CheckAugs();
	function void EquipAug(int);
	function void LevelUpAug(int);
};

#endif
