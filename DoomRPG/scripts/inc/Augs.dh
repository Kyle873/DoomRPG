#ifndef _AUGS_DH_
#define _AUGS_DH_

#include "Defs.dh"
#include "Globals.dh"
#include "Stats.dh"

library("Augs");

int MaxAugSlots;

extern Variable MapArray str[MAX_AUGS][AUG_LEVELS + 1] AugData;
extern Variable MapArray int[MAX_AUGS] AugLevelMax;

extern function void CheckAugSlots();
extern function void CheckAugs();
extern function void EquipAug(int);
extern function void LevelUpAug(int);

#endif
