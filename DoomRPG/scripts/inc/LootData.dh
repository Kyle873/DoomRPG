#ifndef _LOOTDATA_DH_
#define _LOOTDATA_DH_

AddressSpaceDecl map_array LootSpace;

// Loot Definition
struct LootItem
{
    str Actor;
    str Name;
    str Icon;
    int Effect;
};

extern variable LootSpace LootItem[MAX_LOOT] LootData;

#endif
