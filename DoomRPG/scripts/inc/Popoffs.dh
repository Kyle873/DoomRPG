#ifndef _POPOFFS_DH_
#define _POPOFFS_DH_

extern
{
    script void DamageNumbers();
    script void InfoPopoffs();
};

extern
{
    function void Popoff(int, int, int);
    function int GetDigit(int, int);
};

#endif
