#ifndef _STIMS_DH_
#define _STIMS_DH_

#include "Defs.dh"

library("Stims");

extern
{
	variable map_array str[MAX_COMPONENTS] CompoundNames;
	variable map_array int[MAX_COMPONENTS] CompoundColors;
};

extern "ACS"
{
	script void UseStim() net;
};

extern
{
	script void AddCompoundScript(int, int);
	function void AddCompound(int, int);
	function void CheckStim();
	function void MixStim(int);
	function void SetStim(int);
};

#endif
