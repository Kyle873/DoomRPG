#ifndef _MAP_DH_
#define _MAP_DH_

extern
{
    variable map_register int Kills;
    variable map_register int Items;
    variable map_register int SecretsFound;
    variable map_register bool ParBonus;
};

extern "ACS"
{
    script void MapInit() open;
    script void Exit(bool) @ 30000;
};

extern
{
    function bool CheckVisited(str);
};

#endif
