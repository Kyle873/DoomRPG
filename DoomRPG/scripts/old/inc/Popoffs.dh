#ifndef _POPOFFS_DH_
#define _POPOFFS_DH_

extern
{
    script void DamageNumbers();
    script void InfoPopoffs();
    script void ModulePopoffs();
};

extern
{
    function void Popoff(int, int, int, str, bool);
    function int GetDigit(int, int);
    function int GetDamageNumbersDelay();
};

#endif
