#include <zcommon.acs>

#include "RPG.dh"
#include "Menu.dh"
#include "Stats.dh"
#include "Skills.dh"

script Loop()
{
	while (1)
	{
		MenuLoop();
		
		InMenu = true;
		
		Delay(1);
	};
};

script Init() enter
{
	if (!firstRun)
	{
	};
	
	// Version Info
	// Log(s:"\cnDoom RPG ", s:Version, s:" (", s:TimeStamp, s:") ", s:"loaded!");
	
	ACS_NamedExecuteAlways("Loop", 0);
};
