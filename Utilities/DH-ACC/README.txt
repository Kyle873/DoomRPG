David's ACS C Compiler (DH-acc)


DH-acc is a multi-language, multi-target compiler aimed at computer game
scripting engine virtual machines that accept low-level instruction data (such
as bytecode or assembly). Its primary focus is bringing C/C++ semantics and
principles to those targets.

